---
title: ""
pagetitle: "Angira Shukla"
---

::: {.profile-header}
![](assets/headshot.jpg){.headshot fig-alt="Angira Shukla"}

::: {.intro}
# Angira Shukla

Hi! I'm an incoming PhD student at the Department of Economics at the University of Chicago. My research interests are development economics, political economy, and environmental economics. Prior to this, I worked at the Becker Friedman Institute as a predoc with Oeindrila Dube. I've designed behavioral experiments and RCTs, and worked with satellite and geospatial data and large-scale administrative datasets.

Before Chicago, I did my master's in International and Development Economics at the University of San Francisco and my bachelor's in Economics from Ramjas College, University of Delhi.

::: {.contact-links}
[✉ Email](mailto:angira.shukla@gmail.com)
[CV](cv.qmd)
[GitHub](https://github.com/angirashukla)
[LinkedIn](https://www.linkedin.com/in/angira-shukla/)
[Twitter/X](https://x.com/ShuklaAngira)
:::

:::
:::

In the wild, you can spot me data sleuthing, finding exogenous variation, hosting movie nights, or doing the occasional photography. Here's today's pick of my photos.

<div class="potd" id="potd"></div>

```{r}
#| echo: false
#| output: asis
#| message: false
#| warning: false

# Expose the full photo list to the browser; JS picks 5 per day (date-seeded).
imgs <- list.files(
  "photos",
  pattern = "\\.(jpg|jpeg|png|webp|JPG|JPEG|PNG|WEBP)$",
  full.names = FALSE
)
if (length(imgs) > 0) {
  arr <- paste0('"photos/', imgs, '"', collapse = ",")
  cat(sprintf('<script>const POTD_ALL=[%s];</script>\n', arr))
}
```

```{=html}
<script>
(function () {
  // Deterministic PRNG (mulberry32) so the same 5 photos show all day, per date.
  function mulberry32(a) {
    return function () {
      a |= 0; a = a + 0x6D2B79F5 | 0;
      var t = Math.imul(a ^ a >>> 15, 1 | a);
      t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
      return ((t ^ t >>> 14) >>> 0) / 4294967296;
    };
  }
  function render() {
    var box = document.getElementById("potd");
    if (!box || typeof POTD_ALL === "undefined" || !POTD_ALL.length) return;
    var d = new Date();
    var seed = d.getFullYear() * 10000 + (d.getMonth() + 1) * 100 + d.getDate();
    var rnd = mulberry32(seed);
    var pool = POTD_ALL.slice();
    for (var i = pool.length - 1; i > 0; i--) {      // Fisher–Yates shuffle
      var j = Math.floor(rnd() * (i + 1));
      var tmp = pool[i]; pool[i] = pool[j]; pool[j] = tmp;
    }
    box.innerHTML = "";
    pool.slice(0, Math.min(5, pool.length)).forEach(function (p) {
      var im = document.createElement("img");
      im.src = encodeURI(p); im.alt = "Photo of the day";
      im.className = "zoomable"; im.loading = "lazy";
      box.appendChild(im);
    });
  }
  if (document.readyState !== "loading") render();
  else document.addEventListener("DOMContentLoaded", render);
})();
</script>
```
